language: python
python:
  - 2.6
  - 2.7
services:
  - mongodb
before_script:
  - pip install pip8 Flask Flask-Login Flask-Bcrypt Flask-SeaSurf Flask-Cache Flask-Admin pymongo mongokit Flask-MongoKit blinker PyYAML pygments --use-mirrors
install:
  - pip install -e . --use-mirrors
script: 
  - pep8 --repeat src/
